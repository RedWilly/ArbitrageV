i will prefer you use the old version of the contract(NArb old) instead of the new one(NArb) 
since the new version i  have not tested it.

to use the old version removed all the forge test and the console from the contract as am too lazy to do so